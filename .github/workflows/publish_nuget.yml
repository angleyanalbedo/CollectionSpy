name: Publish to NuGet

on:
  push:
    tags:
      - 'v*' # Trigger on version tags, e.g. v1.0.0
  workflow_dispatch: # Allow manual trigger

jobs:
  publish:
    runs-on: windows-latest
    permissions:
      id-token: write # Required for NuGet.org Trusted Publishing
      contents: read  # Required for actions/checkout
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: |
          8.0.x
          6.0.x

    - name: Setup NuGet.exe
      uses: nuget/setup-nuget@v1
      with:
        nuget-version: 'latest'
          
    - name: Restore dependencies
      run: dotnet restore TrapLibrary/Debugging.Traps.csproj
      
    - name: Pack
      # Automatically sets the package version from the git tag if available (e.g., v1.0.0 -> 1.0.0)
      # We removed --no-build to ensure the Version property is applied correctly during a fresh build
      run: |
        $Version = "${{ github.ref_name }}".TrimStart('v')
        if ("${{ github.ref_type }}" -eq "tag") {
            dotnet pack TrapLibrary/Debugging.Traps.csproj --configuration Release --output ./nupkgs /p:Version=$Version
        } else {
            dotnet pack TrapLibrary/Debugging.Traps.csproj --configuration Release --output ./nupkgs
        }
      shell: pwsh
      
    - name: Verify Package Creation
      run: ls ./nupkgs
      
    - name: Publish to NuGet (Trusted Publishing)
      # Use Get-ChildItem to resolve the file path explicitly
      run: |
        foreach($file in (Get-ChildItem "./nupkgs/*.nupkg")) {
            nuget push $file.FullName -Source https://api.nuget.org/v3/index.json -SkipDuplicate -ApiKey "TrustedPublishing"
        }
